# 📝 دليل نشر المقالات - Blog Publishing Guide

## 🎯 كيفية نشر المقالات - How to Publish Articles

### الخطوة 1️⃣: الوصول لـ Dashboard
1. انتقل إلى: `/dashboard/admin/blog`
2. ستجد قائمة بجميع المقالات (المنشورة والمسودات)

### الخطوة 2️⃣: إنشاء مقالة جديدة
1. اضغط على زر **"مقالة جديدة"** (New Article)
2. ملأ النموذج التالي:

```
📌 العنوان (Title) - English
📌 العنوان (العربية) - Arabic
📌 الملخص (Excerpt) - English & Arabic
📌 المحتوى الكامل (Content) - English & Arabic
🏷️ الفئة (Category): Training, Nutrition, Psychology, Fitness, News, Tips
🔖 الوسوم (Tags): مفصولة بفاصلة (comma-separated)
⭐ مقالة مميزة (Featured Article)
✅ نشر الآن (Publish Now)
```

### الخطوة 3️⃣: حفظ المقالة
1. اضغط **"حفظ المقالة"**
2. ستظهر رسالة تأكيد النجاح

### الخطوة 4️⃣: إدارة المقالات
من لوحة التحكم يمكنك:
- 👁️ **عرض/إخفاء**: اضغط على أيقونة العين لنشر/إلغاء نشر
- ✏️ **تعديل**: اضغط على قلم التعديل
- 🗑️ **حذف**: اضغط على سلة المهملات

---

## 🔌 Backend API Endpoints Required

يجب إضافة النقاط التالية في الـ Backend:

### 1. إنشاء مقالة
```
POST /api/v1/blog/articles
Body: {
  "title": "string",
  "titleAr": "string",
  "content": "string",
  "contentAr": "string",
  "excerpt": "string",
  "excerptAr": "string",
  "category": "string",
  "categoryAr": "string",
  "tags": ["string"],
  "featured": boolean,
  "published": boolean
}
```

### 2. الحصول على جميع المقالات
```
GET /api/v1/blog/articles?published=true&limit=50&page=1
Response: {
  "articles": [...],
  "total": number,
  "page": number,
  "limit": number
}
```

### 3. الحصول على مقالة واحدة
```
GET /api/v1/blog/articles/:id
Response: {
  "article": {...}
}
```

### 4. تحديث مقالة
```
PATCH /api/v1/blog/articles/:id
Body: {
  "title": "string",
  "titleAr": "string",
  // ... other fields
}
```

### 5. حذف مقالة
```
DELETE /api/v1/blog/articles/:id
```

### 6. نشر مقالة
```
POST /api/v1/blog/articles/:id/publish
```

### 7. إلغاء نشر مقالة
```
POST /api/v1/blog/articles/:id/unpublish
```

---

## 📱 واجهة المستخدم - User Interface

### صفحة المدونة (`/blog`)
- عرض جميع المقالات المنشورة
- البحث عن مقالات
- تصفية حسب الفئة
- شبكة عرض أنيقة وسريعة

### صفحة المقالة الواحدة (`/blog/[id]`)
- عرض كامل المقالة
- معلومات المقالة (التاريخ، وقت القراءة، العدد المشاهدات)
- مشاركة المقالة
- الوسوم

---

## ⚙️ معلومات تقنية - Technical Info

### الملفات الرئيسية:
```
📁 types/blog.ts              # تعريفات البيانات
📁 services/blog.ts           # التواصل مع API
📁 components/blog/           # مكونات المدونة
📁 app/dashboard/admin/blog/  # Dashboard الإدارة
📁 app/blog/                  # صفحات المدونة العامة
```

### المتطلبات:
- React 19+
- Next.js 15+
- TypeScript
- Tailwind CSS

---

## ✅ Checklist قبل الإطلاق

- [ ] تم إضافة كل النقاط في الـ Backend
- [ ] تم اختبار إنشاء مقالة جديدة
- [ ] تم اختبار البحث والتصفية
- [ ] تم اختبار النشر/إلغاء النشر
- [ ] تم التحقق من التصميم على جميع الأجهزة

---

## 🆘 حل المشاكل - Troubleshooting

| المشكلة | الحل |
|--------|------|
| لا تظهر المقالات | تحقق من أن المقالات منشورة `published: true` |
| خطأ عند الحفظ | تأكد من ملء جميع الحقول المطلوبة |
| البحث لا يعمل | جرب بحثاً بكلمات عربية أو إنجليزية |

